name: Generar version manifest de carpeta source

on:
  push:
    branches:
      - main
      - master

jobs:
  generar-version-manifest:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Ejecutar script de hash
        run: python source/macros/_hash_version.py

      - name: Guardar version manifest en el repositorio
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add source/version_manifest.yml
          git commit -m "Actualizar version manifest" || echo "No hay cambios para commitear"
          git push
